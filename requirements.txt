# Antique Mystery Podcast - Python Dependencies
# Compatible with Python 3.10+

# Deep Learning Framework
torch>=2.0.0
torchaudio>=2.0.0

# Audio Processing
pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0,<2.0.0

# Document Parsing
python-docx>=1.1.0

# Web UI Framework
gradio>=4.0.0

# Configuration Management
pyyaml>=6.0.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# NLP and Tokenization
nltk>=3.8
transformers>=4.30.0

# TTS Engines (will be installed separately based on compatibility testing)
# Option 1: Chatterbox TTS (MIT License)
# Installation: Follow instructions from https://github.com/resemble-ai/chatterbox
# Note: May require special M1 Mac setup

# Option 2: GPT-SoVITS (MIT License) - M1 Mac Fallback
# Installation: pip install gpt-sovits-python
# Note: Better M1 compatibility

# Optional XTTS engine (Coqui - CPML license)
TTS>=0.22.0

# ffmpeg is required but installed separately via system package manager
# macOS: brew install ffmpeg
# Linux: apt-get install ffmpeg
